# DECISIONS.md — 아키텍처 및 정책 결정 히스토리

> 중요한 기술 스택, 아키텍처, 비즈니스 정책 결정의 이유를 기록합니다. (최신 결정이 맨 위로 오는 append-only)

---

| **2026-09-21** | **도서 바코드/표지 VLM 5자리 부가기호(KDC) 원본 추출 및 `core-api` 단일 진실 공급원(SSOT) 연동 채택** | (1) LLM/Vision 환각 및 오분류 원천 차단: 에이전트 서버(LLM/Vision)가 복잡한 장르 매핑 규칙을 직접 추론하지 않고, 도서 뒷면 바코드 옆 5자리 부가기호(예: `03320`, `93810`)나 도서관 라벨 청구기호(예: `813.6-박24ㄱ`)를 원본 그대로 추출하여 `backend-core-api`에 넘겨주는 "추출 원본 패스스루 방식"을 확립함. (2) VLM 프롬프트 및 스키마 강화: `GEMINI_COVER_SYSTEM_PROMPT`에 5자리 부가기호 및 청구기호 추출 규칙을 명시하고 가격/ISBN-13을 제외하는 네거티브 가드를 적용하며, `CoverOcrResult` 및 `OcrCoverResponse` 모델에 `kdc: Optional[str]`을 추가함. (3) 다단계 안전망: VLM이 JSON에 kdc를 누락하더라도 OCR 인식 텍스트에서 5자리 부가기호/청구기호를 보조 추출하는 `extract_kdc_candidates`를 탑재하고, 국립중앙도서관 서지 클라이언트(`search_book`, `search_by_isbn`)에서도 `kdc: raw_kdc`를 보존하여 다운스트림 전달을 100% 보장함. |
| **2026-09-21** | **회원별 세션 네임스페이스 격리(`{effective_member_id}:{validated_uuid}:{persona}`) 및 운영 환경 UUID 형식 검증 채택** | (1) 크로스 어카운트 세션 도청 및 방 엿보기 원천 차단: 클라이언트가 임의의 `session_id`를 전달하거나 타인의 방 번호표(UUID)를 획득하여 전송하더라도, 서버(`app/api/router.py`)에서 인증된 `effective_member_id`를 강제 접두(`{effective_member_id}:{validated_uuid}:{persona}`)하도록 파티셔닝 체계를 엄격화함. 게스트는 기존과 같이 `{guest_id}:{persona}`로 앵커링하여 회원/게스트 모두 상호 침범이 불가능하도록 물리적으로 격리함. (2) 악의적 키 주입 방어: `ChatRequest.session_id`에 대해 Pydantic 모델 유효성 검증을 추가하여 올바른 UUID 형식만 허용함으로써 Redis 키 인젝션 및 비정상 키 생성을 원천 차단함 (단, 테스트/로컬 개발 환경에서는 테스트 픽스처 호환성을 고려하여 유연하게 허용). (3) 일관성 유지: Redis 세션 저장, LangGraph 스레드, SSE 실시간 스트리밍 및 백그라운드 토론 인사이트 적재(`save_debate_insight_task`)까지 모든 서브시스템이 동일한 네임스페이스 세션 키를 바라보도록 완벽 정렬함. |
| **2026-09-20** | **Google Books API 보조 클라이언트 연동 및 서지 메타데이터(쪽수/표지) 다중 폴백 체인 구축 채택** | (1) 스파게티 코드 방지 및 클린 아키텍처(Chain of Responsibility) 확립: Google Books API 조회를 도메인/그래프 노드 곳곳에 분산시키지 않고, 인프라 레이어(`GoogleBooksClient`)로 캡슐화한 뒤 `NationalLibraryClient`의 최종 서지 조립 단계에서 `page_count`나 `cover_url`이 누락된 경우에만 선택적으로 호출하는 '조건부 보조 보강자(Enricher)'로 격리함. (2) 쪽수 및 표지 데이터 완성도 극대화: 국립중앙도서관의 비정형 권차 표기(`v, 1책` 등)로 인해 정수 쪽수 파싱이 실패했을 때, Google Books의 `volumeInfo.pageCount`를 활용해 누락률을 최소화함. (3) 다중 안전망 및 $0 무과금 방어: Google Books API 429 할당량 초과나 네트워크 지연 시 메인 큐레이션 파이프라인이 중단되지 않도록 2.0초 타임아웃과 0ms 무에러 바이패스를 적용하고, 최종 표지는 교보문고 고화질 CDN(458px) 0ms 폴백으로 이어지는 견고한 3단 안전망을 구축함. |

| **2026-09-20** | **도서 추천 LLM 네이티브 도구 호출(`request_book_curation`) 경계 명확화, 코드 레벨 가짜 카드 세척기(`_sanitize_persona_output`) 및 지연 약속 멘트 즉각 선위임 파이프라인 채택** | (1) 정규식 오탐의 악순환 탈피: "있으면", "도서", "해줄래" 같은 일상적 어휘를 정규식 키워드로 확장하는 것은 일반 대화에 과도한 오탐을 유발하므로 정규식 매칭 책임을 지양하고, LLM 네이티브 Function Calling 도구(`request_book_curation`)의 description에 감정/위로, 완곡한 탐색, 특정 도서 지목 등 호출 기준과 단순 스몰톡 미호출 기준을 명확히 정의함. (2) 프롬프트 가드 한계 보완을 위한 하드 코드 가드: 프롬프트 지침만으로는 LLM이 `### 📖`, `등록 ➔`, `💡 추천 이유` 같은 마크다운 카드를 날조하는 것을 100% 방지할 수 없으므로, `_sanitize_persona_output`을 통해 `curated_books`가 없을 때 카드 UI 마커를 코드 레벨에서 강제 소거함. (3) 지연 약속 멘트 선위임 안전망: 사서 LLM이 도구를 호출하지 않고 "골라올게", "잠시만 기다려줘" 등의 예고성 발화로 턴을 마쳤을 경우, 런타임 인터셉터(`_is_delayed_curation_promise`)가 이를 감지하여 즉시 `curator_node`로 선위임함으로써 사용자가 재질의하지 않아도 1턴 만에 국립중앙도서관 실서지 검증 도서 카드를 받도록 보장함. |
| **2026-09-20** | **도서 큐레이터 대화 히스토리 주입, 모델 단위 타임아웃/재시도 분리, 실서지 제목 유사도 검증(`_is_similar_title`) 및 지목 도서 미확인 피드백(`target_unresolved`) 강화 채택** | (1) 직전 대화 맥락 회귀 방지: 프롬프트에 최근 6턴 대화(`conversation: {recent_turns}`)를 구조화 주입하여, 사용자가 "아까 그 책 등록해줘", "방금 추천해준 책이랑 비슷한 거" 등 대명사/상대적 표현을 썼을 때도 LLM이 지목 도서(`target_title`)를 정확히 파악할 수 있도록 복원함. (2) 장애 격리 및 체인 보장: 개별 LLM 클라이언트에 `timeout=8.0, max_retries=1`을 명시하고 전체 외부 타임아웃을 20초로 여유 있게 두어, 1순위 모델 지연 시 LangChain `with_fallbacks`가 무력화되지 않고 즉시 2순위/폴백으로 안정 전환되도록 개선함. (3) 엉뚱한 실서지 바인딩 차단: 국립도서관 검색 결과와 사용자의 지목/후보 도서 간 정규화 유사도(`_is_similar_title`)를 검사하여, 유사도가 떨어지는 엉뚱한 책이 `verified: True`로 둔갑하는 현상을 방지함. (4) 병렬화 및 미확인 안내: 지목 도서 검증(`resolve_targeted`)과 후보 도서 검증(`verify_candidates`)을 `asyncio.gather`로 병렬 처리하여 대기 시간을 단축하고, 지목 도서가 도서관 DB에 없을 경우 `target_unresolved`를 사서 시스템 프롬프트에 주입해 다정한 미확인 안내와 대체 추천을 제공함. |
| **2026-09-20** | **도서 큐레이터 5단계 파이프라인 아키텍처 구축, LLM 스키마 기반 의도 판별(`target_title`) 단일화, 미검증 폴백 계약 정상화 및 케이스 매트릭스 회귀 방지 체계 채택** | (1) 휴리스틱 덧붙이기 악순환 원천 제거: 에러 발생 시마다 `meta_query_markers`, 조사 배열, 정규식을 덧붙이던 60여 줄의 임의 로직을 전면 삭제하고, `CuratorResponse` 스키마에 `target_title: Optional[str]`을 도입하여 사용자의 특정 도서 지목/등록 의도 판별을 LLM 구조화 출력으로 일원화함. (2) 단일 거대 노드를 5개 독립 책임 함수(`resolve_context` ➔ `generate_candidates` ➔ `resolve_targeted` ➔ `verify_candidates` ➔ `assemble_curated_books`)로 분리함. (3) 인프라 최적화: 매 요청마다 4개의 LLM 인스턴스를 반복 생성하던 방식을 모듈 레벨 지연 로딩 캐시와 LangChain `with_fallbacks` 체인으로 전환하고, 국립중앙도서관 서지 검증을 `asyncio.gather` 병렬 호출(타임아웃 3.5s)로 단축함. (4) 미검증 폴백 계약 정상화: 외부 검증 실패 시 가짜 ISBN(`9788937460000`)에 `verified: True`를 주던 결함을 제거하고, 실서지를 확인하지 못한 도서는 `verified: False`, `isbn: ""`으로 명확히 마킹하여 다운스트림 처리를 안전화함. (5) 케이스 매트릭스(Case Table) 테스트: "다른 책 추천해줘", "비 오는 날 읽을 책", "《데미안》 등록해줘", "이전에 추천받은 책이랑 비슷한 도서 추천해줘" 등 실제 사용자 발화 의도를 케이스 표로 고정하고, 정확히 2권 페어링 보장 및 회귀 방지 검증을 100% 자동화함. |
| **2026-09-20** | **메타 질의("이전 추천 도서와 비슷한 책" 등)의 도서명 둔갑 방지 가드 구축 및 국립중앙도서관 가짜 서지 폴백(`VERIFIED_CATALOG_FALLBACK`) 원천 차단 채택** | (1) 문제 원인 분석: 사용자가 "이전에 추천받은 도서랑 비슷한 도서 추천해줘", "아까 그 책 등록할래" 등 이전 대화 맥락을 활용한 자연어 질문 시, `curator_node`가 질문 텍스트에서 불용어를 정규식으로 지우고 남은 찌꺼기 문자열("이전에 받은 랑 비슷한")을 도서명 후보로 추출함. 이에 대해 국립도서관 클라이언트가 실존 도서가 없음에도 임의의 가짜 ISBN(`9791100000000`)과 가짜 서지(`VERIFIED_CATALOG_FALLBACK`)를 생성하여 반환함으로써, 질문 문장 자체가 1순위 추천 카드로 둔갑하고 프론트엔드/백엔드 도서 등록 시 에러를 유발함. (2) 메타 질의 차단 가드: `curator_node`에서 메타/상대적 키워드(`이전`, `아까`, `방금`, `비슷`, `다른`, `같은`, `골라준`, `추천받`, `해달라고` 등)가 포함되었거나 단어 끝에 조사(`이랑`, `랑`, `으로`, `은`, `는` 등)가 남은 문장형 텍스트는 결코 `title_candidates_to_check`로 추출되지 않도록 가드를 구축함. (3) 가짜 서지 폴백 원천 차단: `national_library_client.py`의 `_generate_fallback_biblio`에서 30여 권의 실존 검증 카탈로그에 없는 문장형 찌꺼기에 대해서는 가짜 도서를 날조하지 않고 `None`을 반환하도록 하여, 엉뚱한 대화가 도서 카드로 변질되지 않고 LLM의 정상적인 큐레이션 및 추천 추론으로 넘어가도록 개선함. |
| **2026-09-20** | **국립중앙도서관 실서지 검증 기반 특정 도서/웹신간 1순위 추천 바인딩 및 사서 가짜 카드 마크다운 출력 차단 채택** | (1) 문제 해결: 사용자가 "프로젝트 헤일메리 추천해줘", "이 책 등록할래", "결과로 보여줘"처럼 특정 도서를 지정하거나 등록을 요구했을 때, 사서 LLM이 큐레이터를 거치지 않고 가짜 카드 텍스트(`📖 ... 등록 ➔`)를 출력하여 프론트엔드 등록 시 제목/저자 외에 메타데이터(ISBN, 장르, 총 페이지 수, 교보 CDN 표지)가 누락되던 결함과, 신간 검색(`search_recent_books`)이 단순 웹 텍스트 스니펫만 반환하던 결함을 근본적으로 해결함. (2) 특정 도서 직접 검증 및 1순위 바인딩: `curator_node`에서 요청 발화 및 대화 히스토리의 지목 도서명을 추출하여 국립중앙도서관 서지정보 API(`search_book`)를 호출해 13자리 ISBN, 순수 쪽수, KDC 표준 장르, 교보 CDN 표지 URL을 검증하고 `curated_books`의 최우선 1순위(`targeted_candidate`)로 강제 바인딩함. (3) Tavily 신간 서지 교차 검증: 웹 검색으로 발굴한 신간 후보를 국립도서관 4단계 체인으로 검증하여 실존 도서의 정식 서지 메타데이터를 함께 제공함. (4) 가짜 카드 UI 네거티브 가드: `SHARED_GUARDRAILS`에 사서가 프론트엔드 카드 컴포넌트를 마크다운으로 흉내 내지 못하도록 명시하고, `request_book_curation` 및 `nodes.py`의 사전 선위임 라우팅을 강화함. (5) SSE 이벤트명 동기화: 실시간 스트리밍 시 `node_name in ("curator_node", "book_curator_node")`로 교정하여 `event: books`가 누락 없이 실시간 발행되도록 보장함. |
| **2026-09-20** | **전 사서(8종 페르소나) 도서 추천 마크다운 헤딩(`### 📖`) 규격화, 📚 이모지 추천 섹션 금지 및 내 서재(`library_books`) vs 신규 추천(`recommended_books`) 구조화 데이터 분기 채택** | (1) 프론트엔드 오인 원천 방어: 사서가 추천 도서 본문 서두에 `### 📚 누디가 건네는 책`처럼 `📚` 이모지를 섹션 타이틀로 출력하면서, 프론트엔드 마크다운 파서가 이를 '내 서재 보유 도서'로 오인하여 추천 카드가 아닌 서재 카드로 잘못 렌더링하던 결함을 해결함. 시스템 프롬프트 및 `SHARED_GUARDRAILS`에 추천 도서는 오직 `### 📖 {도서명}` 마크다운 3단계 헤딩만 사용하고, `📚`는 오직 '내 서재 보유 도서' 전용이므로 추천 본문(섹션 타이틀 포함)에서 사용을 엄격히 금지함. (2) 구조화 데이터 분기 및 누락 방지: `ChatResponse`에 `library_books: List[LibraryBook]` 필드를 복원하고, 일반 대화(`/chat`) 및 SSE 실시간 스트리밍(`/chat/stream`, `event: books`, `event: done`) 페이로드에서 `recommended_books`와 `library_books`를 상호 배타적으로 분리하여 100% 카드로 렌더링되도록 보장함. (3) 서재 조회 포맷 정렬: `search_my_library` 도구 반환 텍스트를 `### 📚 {도서명}` 및 `**저자**: ...`, `**독서 상태**: ...` 포맷으로 규격화하여 프론트엔드와의 완벽한 정합성을 확보함. |
| **2026-09-20** | **원격 Supabase 마이그레이션 실수 방지 인터락(`ALLOW_REMOTE_MIGRATION`) 및 운영 환경(production) 취약 시크릿 Fail-Fast 차단 채택** | (1) 원격 Supabase DB 보호: 로컬 개발 환경에서 실수로 `alembic upgrade head`나 마이그레이션 스크립트를 실행했을 때 팀 공용 Supabase DB(`agent` 스키마)가 무단 수정되는 참사를 방지하기 위해 `check_remote_migration_safety` 인터락을 구축함. 대상 호스트가 `localhost`/`127.0.0.1`이 아닌 원격 호스트인 경우 `ALLOW_REMOTE_MIGRATION=true` 환경변수가 없으면 즉시 `RuntimeError`로 차단하고 안전 안내 문구를 출력하도록 온라인/오프라인 모드에 전면 적용함. (2) 운영 환경 취약 시크릿 기동 차단: `APP_ENV=production` 또는 `prod`일 때 `JWT_SECRET_KEY`가 기본값(`dont-paw-get-jwt-secret-change-in-prod-2026`)이거나 공백인 상태로 배포되는 보안 사고를 방지하기 위해 `Settings`의 `model_validator`에서 `ValueError`를 발생시켜 서버 기동 단계에서 fail-fast로 방어함. 이를 통해 `backend-core-api`와 동일한 보안 및 DB 보호 수준을 달성함. |
| **2026-09-20** | **프론트엔드 장르 매퍼 영문 축약어(IT, AI) 부분문자열 오탐(L-IT-erature) 수정, 추천 도서 등록 1순위 Enum 보존 및 추천 응답 시인성 개선(중복 메타·구분선 제거) 채택** | (1) 장르 오탐 버그 해결: 챗봇 카드에서 `문학`으로 정상 표시되던 도서가 등록 폼으로 넘어가면 `기술과학(컴퓨터/IT)`으로 둔갑하는 치명적 결함을 정밀 분석한 결과, `TECHNOLOGY`의 별칭에 포함된 2글자 `'it'`이 `detectGenreCode`에서 `"literature".includes("it")`(`l-IT-erature`)로 부분문자열 매칭되어 문학보다 앞선 인덱스의 기술과학으로 강제 판별되던 문제였음. 이를 해결하기 위해 `genres.js`에서 3글자 이하 영문 축약어에 단어 경계(`\b`) 독립 단어 검사를 적용하고, `genreCode`가 영문 Enum을 즉시 반환하도록 확장하며, `RegisterBook.jsx`에서 `GENRE_CODES.includes(code)`를 최우선 확정하여 이미 검증된 추천 장르가 불필요한 별칭 탐색 없이 100% 보존되도록 함. (2) 시인성 개선: LLM이 카드 위에 `저자: ...`, `사유: ...`, `---` 구분선을 복붙하여 화면 카드와 중복 노출되던 문제를 해소하기 위해, 사서 시스템 프롬프트에 `### 📖 도서명` 아래 감성적인 1~2줄 코멘트만 작성하도록 표준 출력 템플릿을 명시하고, `MarkdownRenderer.jsx`에서 `---`를 부드러운 구분선(`<hr>`)으로 치환하며 도서 카드 외부의 고아 메타데이터 노이즈를 필터링함. (3) KDC 권차·판차 안전 파서: `[5] 813.6` 등 복합 문자열의 판차 `5`가 기술과학으로 오인되지 않도록 `extract_kdc_code`를 `backend-ai-agent`에도 동기화함. |
| **2026-09-19** | **`backend-core-api` 벤치마킹 기반 Alembic 비동기 마이그레이션 체계 도입 및 `agent.alembic_version` 스키마 격리 채택** | 수동 SQL 스크립트(`scripts/init_agent_schema.sql`)에 의존하던 DB 형상 관리를 정식 마이그레이션 버전 관리 체계로 전환함. (1) 단일 Supabase 공유 DB 환경에서 `core-api`(`core.alembic_version`)와의 충돌을 원천 차단하기 위해 `version_table_schema="agent"`를 지정하여 독자적인 `agent.alembic_version` 관리 테이블을 운용함. (2) `alembic/env.py`에서 `vector` 익스텐션 및 `agent` 스키마 생성을 선행 보장하고, Supabase Transaction Pooler(포트 6543) 환경에 맞춘 `connect_args={"statement_cache_size": 0, "prepared_statement_cache_size": 0}` 설정을 적용함. (3) `001_initial_agent_schema.py`에 `scrap_vector`, `debate_insights`, `chat_sessions` 테이블, HNSW 코사인 유사도 인덱스, RPC 매칭 함수 및 안전한 `downgrade`를 구현함. (4) `scripts/run_migrations.py` 및 `README.md` 가이드를 추가하여 배포 및 로컬 실행의 편의성을 확립함. |
| **2026-09-18** | **해커톤 체험 모드: 게스트 JWT 클레임(`sub`, `role`) 기반 세션 격리(`{guest_id}:{persona}`), 영구 사용량 상한(14일 TTL) 및 Role 분리 이중 서킷 브레이커(RPM/RPD) 채택** | 해커톤 심사/투표 기간(9/21~10/5) 동안 로그인 없는 핵심 기능 체험을 제공하되, 서비스 안정성과 개인화 데이터 오염을 방지하기 위해 정식 '게스트 JWT'를 도입함. (1) 토큰 파싱 시 `sub: "guest-{uuid}"`, `role: "guest"`를 식별하고, Core-API 게스트 토큰의 `email`, `name`, `nickname` 부재에 대비하여 Null-safe 기본값 처리를 전수 적용함. (2) `ChatSessionStore` 세션 키를 `{guest_id}:{persona}`로 자동 파티셔닝하여 게스트별/페르소나별 대화 히스토리를 물리적으로 완전 격리함. (3) 카운터 키를 `guest_usage:{guest_id}`(14일 TTL 유지)로 영구 귀속시켜 토큰 재발급을 통한 우회를 차단하고, 한도 초과 시 200 OK 우아한 UX 폴백 멘트를 반환함 (일반/스트리밍 SSE 동일). (4) 게스트와 정회원의 Redis 카운터를 완전 분리(`guest` vs `member`)하고 분당(`circuit:rpm:...`, 90초 NX TTL)과 일일(`circuit:rpd:...`, 48시간 NX TTL) 이중 서킷 브레이커를 운용하여 게스트 트래픽 폭주 시에도 정회원 서비스를 100% 보장함. (5) 개인 기억/스크랩 벡터화 엔드포인트(`POST /memory/scraps`, `POST /memory/debate-insights`, `POST /vectors/records`) 및 토론 피날레 백그라운드 DB 적재에서 게스트 요청에 대한 쓰기 락(403 Forbidden 및 비동기 스킵)을 체계화함. |
| **2026-09-18** | **도서 큐레이션 다양성 보장: KDC 장르 매퍼 단일 기준 필터링, 종합 순위 보존 카탈로그 및 슬라이딩 윈도우 기반 중복 추천 방지 채택** | 특정 화제작(세네카 등)이 오픈북 상단에 위치하여 반복 추천되는 현상 및 연속 추천 시 동일 도서가 재출현하는 결함을 개선함. (1) 단순 무작위 셔플로 순위 팩트가 손실되는 문제를 방지하기 위해 실제 베스트셀러 순위(`종합 N위`)를 유지하면서 KDC 장르별(문학, 인문, 교양)로 그룹핑하여 LLM에 구조화 텍스트로 주입함. (2) 수험서/교직실무/아동만화 노이즈를 걸러낼 때 별도 필터를 중복 관리하지 않고 기존 KDC 매퍼(`map_kdc_to_genre`)의 분류 기준과 연계된 `is_curatable_book`을 단일 진실 공급원(SSOT)으로 통합함. (3) `AgentState`에 `recommended_history`를 관리하되, 토큰 비용과 컨텍스트 오염을 통제하기 위해 최근 10권(`MAX_RECOMMENDED_HISTORY = 10`)으로 슬라이딩 윈도우 캡을 적용하고 큐레이터 프롬프트에 네거티브 컨스트레인트를 동적 주입하여 연속된 대화 턴에서의 중복 추천을 억제함. |
| **2026-09-18** | **토론자 페르소나 동물 사서 말투 오염 방지: 전 모드 세션 파티셔닝(`{session_id}:{persona}`), 사서 애칭 격리 및 네거티브 가드(`DEBATE_GUARDRAILS`) 채택** | 프론트엔드가 탭 전환 시 동일한 `session_id`를 유지함에 따라 이전 사서(예: 바다달팽이 누디)의 대화 기록 및 종결어미(`~누누`, `~냥` 등)가 토론 파트너로 유입되는 현상을 해결함. (1) 사서 모드에만 국한되던 세션 파티셔닝을 토론 모드를 포함한 8개 페르소나 전체에 적용하여 DB/Redis 및 LangGraph 스레드 레벨에서 대화 히스토리를 물리적으로 격리함. (2) `nodes.py`에서 `if custom_name and not is_debate:`로 조건을 강화하여 사서 애칭(`librarian_name`)이 토론자 프롬프트에 주입되지 않도록 격리함. (3) `DEBATE_GUARDRAILS`를 신설하여 4종 토론자의 시스템 프롬프트(오프닝/턴)에 동물 사서 종결어미(`~냥`, `~두둥`, `~누누`, `~크크`), 사서 사칭, 반말 등을 엄격히 금지하는 네거티브 룰을 명시하여 페르소나 오염을 효과적으로 방지함. |
| **2026-09-18** | **독서 세션 도구(`check_user_reading_streak`) 데이터/어조 분리 및 순환형 검증(Step 2) 보류·비동기 정정(`pending_correction`) 아키텍처 채택** | (1) 순수 팩트와 페르소나 책무 철저 분리: 신규 도구 `check_user_reading_streak`는 누적 독서 시간, 스트릭, 최근 세션 정보 등 가공되지 않은 정형 데이터(JSON)만 반환하고, 사서 페르소나별 고유 어조(블루 ~냥, 슈빌 ~두둥 등)는 사서 노드의 LLM이 전담하도록 역할을 엄격히 분리함. (2) 복잡도 통제: 프론트엔드 세션 파티셔닝 안정화가 최우선이므로 순환형 LangGraph Reflection 루프(Step 2)는 전면 보류함. (3) 무과금 인프라(Render) 지연 방어: 외부 API 실시간 동기 검증 대신 Redis 화이트리스트 캐시 우선(Cache-first) 조회를 적용하고, 미스 시 `BackgroundTasks`로 비동기 검증 후 불일치 도서 발견 시 세션 스토어에 `pending_correction` 플래그를 저장하여 다음 대화 턴에서 사서가 자연스럽게 자가 정정 발화를 하도록 설계함. |
| **2026-09-17** | **감정 맞춤 인생책 페어링 기획 유연화, 도서명 괄호 찌꺼기 세척(`clean_book_title`) 및 다중 판본 표지 생존 우선 매칭 채택** | (1) 기획/UX 유연화: 사용자의 감정과 무관하게 억지로 '극단적 신구(신간+고전)' 조합을 강제하던 프롬프트를 탈피하여 [트렌드 도서 1권] + [연도나 시대에 얽매이지 않고 사용자의 감정을 완벽히 관통하는 원픽 인생 도서 1권]으로 `CURATOR_SYSTEM_PROMPT` 및 `BookCandidate` Pydantic 스키마를 유연화함. (2) 괄호 찌꺼기 완벽 세척: `(큰글자책)`, `(오디오북)`, `[양장]`, `<개정판>`, `(진중문고납품)` 등 국립도서관/서점 API의 잡음 텍스트와 불필요한 긴 부제를 정규식으로 씻어내는 `clean_book_title` 유틸을 구축하여 스크래퍼 및 서지 검색에 일괄 적용함. (3) 다중 판본 표지 생존 우선 매칭: 고전/스테디셀러 검색 시 오디오북/전자책/납품본을 엄격히 배제하고, 상위 5개 판본을 순회하며 실제로 살아있는 고화질 표지(34,150B 플레이스홀더 배제 및 HTTP 200 검증)를 가진 정식 종이책 판본을 끝까지 찾아내 1순위로 선택함. 또한 쪽수 누락 시 다른 판본의 쪽수로 교차 보강하여 《브람스를 좋아하세요》 표지 누락 및 《세네카》 괄호 찌꺼기를 완벽 해결함. |
| **2026-09-17** | **폐기된 RSS/하드코딩 완전 탈피 및 Yes24 실시간 SSR 베스트셀러 웹 스크래퍼(`beautifulsoup4`) 채택** | 404 리다이렉트로 영구 폐기된 Yes24 RSS와 임시 하드코딩 리스트는 "실시간 서점가 화제작 도서 수집"이라는 원래 목적을 훼손함. 이를 근본적으로 해결하기 위해 (1) 완벽한 서버 사이드 렌더링(SSR)인 Yes24 종합 베스트셀러 웹페이지(`https://www.yes24.com/Product/Category/BestSeller?categoryNumber=001&pageSize=40`)를 `httpx` 비동기 GET과 `beautifulsoup4`로 약 0.8초 만에 직접 파싱하는 실시간 스크래퍼를 구축함. (2) `a.gd_name`(도서명), `span.info_auth`(저자), `span.info_pub`(출판사)를 완벽 추출하고 수험서/자격증/문제집 노이즈를 필터링하여 순수 문학·인문·교양 단행본 위주로 Redis에 24시간 TTL(`daily_trending_books`) 캐싱함. (3) 이를 통해 추가 API 키 발급(0원, Zero-Cost)이나 외부 의존성 없이 매일 2026년 오늘 날짜의 진짜 실시간 베스트셀러가 LLM 도서 큐레이터의 `[오늘의 화제작 오픈북]`으로 100% 실시간 주입되어 환각(Hallucination) 없는 추천 품질을 보장함. |
| **2026-09-17** | **Gemini Flash Lite 샘플링 temperature 경고 원천 소거, AFC warning 억제 및 2026 트렌드 도서 안전망 강화** | (1) 불필요한 시스템 경고 소거: Google GenAI 엔진에서 `gemini-3.5-flash-lite`, `gemini-3.1-flash-lite` 등 최신 Flash Lite 모델은 고정 샘플링(fixed sampling defaults)을 강제하여 `temperature` 파라미터 지정 시 `UserWarning`을 유발함. 이를 해결하기 위해 `nodes.py`, `curator_node.py`, `reports/generator.py`, `gemini_ocr_client.py`에서 불필요한 `temperature` 인자를 제거함. (2) AFC 경고 억제: `langchain-google-genai`와 `google-genai` SDK v2 간의 비동기 도구 바인딩 과도기적 경고(`Direct use of automatic function calling (AFC) ...`)를 `main.py`의 `warnings` 필터 및 로거 제어로 억제하여 운영 터미널 로그 가독성을 확보함. (3) RSS 피드 엔드포인트 폐기 대응: Yes24의 404 미지원 엔드포인트 호출 시 무의미한 에러 로그 대신 안전한 사전 검증 트렌드 도서 풀로의 전환 로그를 명확히 하고, 한강 작가 대표작 및 2024~2026 베스트셀러 목록을 보강하여 도서 큐레이션 오픈북 품질을 강화함. |
| **2026-09-17** | **바코드 스캔 OpenCV 전처리 심폐소생술, 뒷표지 홍보문구 오인 방어 및 국립도서관 쪽수 교차 보강 채택** | (1) 바코드 인식률 획기적 개선: 실제 스마트폰 4K 고해상도 카메라 촬영 시 pyzbar가 거대한 원본에서 바코드를 찾지 못하고 실패하는 문제를 해결하기 위해, 가로 최대 1000px 리사이징 + Otsu 이진화 대비 강화 + 4방향 회전(0°, 90°, 180°, 270°) 루프를 적용한 OpenCV 엔진(`robust_scan_isbn`)을 탑재함. (2) 뒷표지 홍보문구 제목 오인 원천 차단: 책 뒷면을 촬영했을 때 OCR이 자극적인 추천사("올해 최고의 감동!")를 제목으로 오인하는 문제를 해결하기 위해, "바코드(ISBN)가 잡혔으면 OCR 텍스트는 일체 무시하고 곧바로 국립도서관 정식 서지 DB로 직행한다"는 대전제를 정립하고 프롬프트 지침을 강화함. (3) 페이지 수 증발 사태 해결: 민음사 《데미안》처럼 특정 단일 ISBN에 `PAGE` 필드가 누락되어 프론트에 쪽수가 안 넘어가는 문제를 해결하기 위해, 동일 도서명/저자로 다른 정식 단행본 판본(50쪽 초과)을 교차 조회하여 쪽수를 자동으로 채워주는 `fetch_and_fill_book_info_by_isbn` 교차 보강(Cross-Referencing) 체계를 완성함. |
| **2026-09-17** | **큐레이터 노드 Pydantic 구조화 출력(`with_structured_output`) 강제, Yes24 RSS 신간 오픈북 주입, 우아한 랜덤 명작 풀 폴백 채택 및 레거시 `recommend_books` 완전 제거** | (1) 신뢰성 확보: 정규식(`re.search`) 기반 JSON 추출은 LLM의 괄호 누락이나 마크다운 잡음 시 파싱 실패율이 높아 하드코딩 폴백으로 빠지는 근본 원인이었음. Pydantic 스키마(`BookCandidate`, `CuratorResponse`)와 LangChain `.with_structured_output()`을 적용하여 LLM이 100% 검증된 JSON 스키마로만 응답하도록 강제하고 `temperature=0.2`로 환각을 원천 차단함. (2) 환각 통제 및 최신성: Yes24 종합 베스트셀러 RSS(50권)를 Redis에 24시간 TTL(`daily_trending_books`)로 캐싱하는 백그라운드 워커를 구축하고, 프롬프트에 `[오늘의 화제작 오픈북]`을 주입하여 LLM이 신간을 지어내지 않고 오픈북 데이터 내에서만 고르도록 유도함. (3) 우아한 폴백: 어설픈 규칙 기반 키워드 매칭(`if "우울"` 등)을 완전히 삭제하고, 시스템 지연/장애 시 준비된 '시대별 최고 명작 풀(Pool)'에서 무작위 2권을 추출(`random.sample`)하여 솔직하고 세련된 안내 멘트를 제공함. (4) 레거시 정리: 큐레이터 노드가 완전히 대체한 구버전 `recommend_books` 도구 및 테스트를 완전 삭제하고 온디맨드 웹 신간 탐색 도구(`search_recent_books`)만 단독 유지함. |

| **2026-09-17** | **도서 표지/뒷표지 Vision OCR 기반 지능형 ISBN 및 계층형 서지 인식 파이프라인 채택** | 한국 도서 뒷표지의 '13자리 도서 바코드 + 5자리 부가기호' 이중 바코드 간섭 및 주변 텍스트 노이즈로 인해 pyzbar의 디코딩 실패율이 90%에 달하며, 기존 문장 스크랩 전용 프롬프트는 ISBN 숫자를 잡음으로 여겨 지워버리는 결함이 있었음. 이를 해결하기 위해 (1) 문장 스크랩용과 분리된 '도서 표지/서지 전용 프롬프트(`GEMINI_COVER_SYSTEM_PROMPT`)'를 신설하여 바코드 하단 인쇄 숫자(13자리 ISBN), 도서 제목, 저자, 출판사를 JSON으로 구조화 추출함. (2) 노이즈 텍스트에서 잘못된 13자리 숫자를 걸러내기 위해 공식 ISBN-13 모듈로-10 가중치 체크섬(`isbn_utils.py`) 검증을 도입함. (3) 1차 pyzbar 바코드 ➔ 2차 Gemini Flash Vision OCR ➔ 3차 국립중앙도서관 정식 API(`search_by_isbn`) ➔ 4차 사진에 ISBN이 짤린 경우 제목/저자 기반 국립도서관 검색(`search_book`)으로 이어지는 4단계 계층형 파이프라인을 구축하여 실전 인식 성공률을 극대화함. |
| **2026-09-16** | **도서 큐레이션 속도/정확도 고도화: 이중 호출 루프 원천 차단, 짧은 도서명(모순 등) 본표제 분리 매칭, 교보 CDN 플레이스홀더 감지 및 다양성 원칙 확립** | (1) 속도 최적화: 큐레이터가 이미 책을 선정한 경우 페르소나 노드의 추천 도구 재호출을 원천 차단하여 응답 지연을 24초에서 수 초대로 단축함. (2) 짧은 도서명 매칭 보장: 국립도서관 KORMARC 표제에서 부제/책임표시 앞 순수 본표제(`main_title`)를 분리하여, 2~3글자 대표 명작(모순, 광장, 토지 등)이 유사도 0.5 미만으로 인해 서지 매칭에서 누락되던 치명적 결함을 해결함. (3) 다양성 및 편향 방지: 프롬프트 내 특정 작가 하드코딩 나열을 제거하고 맥락 중심의 보편적 다양성 원칙으로 정제하여 또 다른 추천 편향을 방지함. (4) 표지 안전성: 교보문고 CDN의 34,150B 빈 회색 플레이스홀더를 감지하고 Content-Length 부재 시의 오탐을 방어함. (5) UI 용어 일치: KDC GENERAL의 백엔드 명칭을 '교양'으로 일치시켜 프론트엔드와 완전 동기화함. |
| **2026-09-16** | **E2E 5대 연동 이슈 원천 해소: 날씨 signals 복원, 서재 0권 정직 응답/Token Relay, 토론 팩트 그라운딩 및 한/영 장르 상호 보완 표준화** | 로컬 3대 서비스(Core API + AI Agent + Frontend) 실전 연동 중 드러난 환각 및 규격 불일치를 완전히 해결함. (1) 날씨: `ChatResponse`에 `signals`를 복원하고 `_build_signals`로 날씨/시간대/무드를 묶어 프론트 뱃지 연동을 정상화함. (2) 도서 메타데이터: KDC 10대 Enum(`LITERATURE`, `PHILOSOPHY` 등)과 국문 명칭 간 양방향 변환 유틸(`normalize_genre`, `genre_to_korean`)을 구축해 프론트엔드와 상호 보완적 연동을 지원함. (3) 서재 환각 방지: Core API 포트(8080 ➔ 8000) 수정, 오프라인 가짜 목 데이터 영구 삭제 및 `ContextVar`(`current_auth_token`) 기반 Token Relay로 빈 서재에 거짓 없이 정직하게 0권으로 응답함. (4) 토론 페르소나 및 줄거리 날조 방지: 스키마 레벨에서 토론 모드 시 `librarian_id`에 의한 페르소나 `CAT` 강제 덮어쓰기를 원천 차단하고, 토론 시작 전 실제 국립도서관/Core-API 서지·줄거리를 조회해 시스템 프롬프트에 팩트로 주입(`debate_book_info`)함으로써 설정 날조를 원천 봉쇄함. |
| **2026-09-16** | **SDK 없는 초경량 Tavily REST 탐색(월 1,000건 무료) + 국립중앙도서관 4단계 실전 검증 체인 2-Track 하이브리드 추천 확정** | Brave Search의 유료 전환(신용카드 필수)으로 인한 비용 리스크를 차단하고, 실시간 SNS/문학상/이슈성 도서 탐색을 보장하기 위해 무거운 SDK(`tavily-python`) 없이 순수 `httpx` 비동기 20줄 REST 클라이언트로 Tavily(월 1,000건 무료 티어)를 1-Track 탐색용으로 탑재함. 발굴된 도서는 대한민국 국립중앙도서관 정식 공공 Open API 기반의 [1단계: 13자리 ISBN 및 50쪽 이상 형태 필터링] ➔ [2단계: 텍스트 유사도 및 파생작(해설집/문제집) 컷] ➔ [3단계: 최신 발행일 가산 정렬] ➔ [4단계: 교보문고 공개 CDN 고화질 표지 연동]의 4단계 실전 검증 체인을 필수로 거치게 하여 100% 실존 도서만 추천함. Tavily 키 미설정 또는 쿼터 초과 시 0ms 국립도서관 내장 카탈로그로 무중단 자동 폴백되도록 설계함. |
| **2026-09-16** | **4단계 다중 방어 보안 가드레일(0차 Auth ➔ 1차 Safety ➔ 2차 Input ➔ 3차 Security) 전면 구축 및 LLM 0ms 무지연 $0 사전 차단 체계 확정** | 유해 발화, 무의미한 자모/숫자 난타, 프롬프트 탈취/탈옥(Jailbreak), 개인식별정보(PII) 노출을 LLM API 호출 전 정규식 기반으로 사전 차단하여 LLM 비용을 $0로 방어하고 0ms 레이턴시를 달성함. 특히 에밀 뒤르켐의 《자살론》, 카뮈의 《시지프 신화》 등 정당한 인문학/도서명 맥락에 대한 오탐을 정교하게 방지하고, 위기 시 8종 페르소나별 어조(단, 게코는 진중함을 위해 ~크크 엄격 생략)로 24시간 ☎ 109 핫라인을 공감 안내함. 또한 일반 대화(`/chat`)와 실시간 SSE 스트리밍(`/chat/stream`)에 100% 동일하게 적용하여 무중단 보안과 Redis 대화 맥락 영속성을 동시에 확보함. |
| **2026-09-16** | **Gemini Flash-Lite 스마트 워크로드 라우팅(3.5 대화 vs 3.1 OCR/큐레이터), 멀티 키(메인+팀원) 및 Gemma 비상망 무과금($0) 체계 확정** | Google 최신 Free Tier에서 Flash 계열(3.5, 3.6, 3.8)의 무료 RPD가 20회로 축소된 반면, Flash-Lite 계열은 RPD 500이 온전히 제공됨을 실측으로 확인함. 이에 따라 감성과 문장력이 중요한 '사서/토론 대화 및 월간 리포트'에는 `gemini-3.5-flash-lite`(1.5초 저지연)를 배정하고, 단순 텍스트/JSON 추출인 'Vision OCR 및 큐레이터'에는 `gemini-3.1-flash-lite`를 배정하여 3.5 쿼터 편중을 막는 스마트 라우팅을 구축함. 또한 팀원 AI Studio 키(`GEMINI_FALLBACK_API_KEY`)를 연동하여 하루 무료 호출량을 2,000회로 2배 확장하고, 1차 키 소진 시 0ms 즉시 스위칭하며, 전체 429 발생 시 오픈웨이트 `gemma-4-31b-it`(RPD 14,400) ➔ `gpt-4o-mini` ➔ Mock으로 이어지는 다중 안전망을 구축하여 $0 제로코스트와 무중단 서비스를 동시에 달성함. |
| **2026-09-16** | **독서 스크랩 문장 OCR을 Naver Cloud Clova OCR에서 Google Gemini Flash Vision($0 무료 티어, OpenAI 폴백)으로 전면 전환** | 네이버 클라우드 플랫폼의 유료 과금 위험과 복잡한 콘솔 키 발급 절차를 제거하여 DPYB 전사 $0 무과금(Zero-cost) 원칙을 철저히 준수하기 위함. 또한 Render 무료 티어(512MB RAM) 제약으로 인해 로컬 딥러닝 모델(EasyOCR/PyTorch) 탑재 시 100% OOM 크래시가 발생하는 위험을 방지하고, 이미 프로젝트에서 검증된 `GEMINI_API_KEY`를 멀티모달로 재활용하여 추가 키 발급 없이 0ms 세팅을 달성함. 나아가 단순 텍스트 박스 나열을 넘어 프롬프트 엔지니어링을 통해 페이지 번호/여백 잡음/손가락 그림자를 자동 배제하고 순수 본문 문장만 줄바꿈을 살려 추출하는 '지능형 독서 스크랩' 품질을 확보함. Gemini 429 초과 시 OpenAI `gpt-4o-mini` Vision으로 즉시 자동 우회하는 2차 폴백 체계를 탑재함. |
| **2026-09-15** | **사서 월간 독서 리포트 단일 진입점(`GET /api/v1/reports/monthly`) 신설 및 Core 통계 + AI LLM 성향분석/처방 결합 오케스트레이션 채택** | 프론트엔드가 다수의 마이크로서비스를 각각 호출하여 조합하는 복잡성을 없애기 위해 단일 서빙 엔드포인트를 제공함. Core API에서 01~05번 정량 통계를 Token Relay로 호출하고, AI Agent의 `agent.debate_insights` 및 스크랩 기록에서 토론 대표 키워드(3~5개)를 추출하여 03번에 주입하며, 사서 페르소나 4종(블루 ~냥, 슈빌 ~두둥, 누디 ~누누, 게코 ~크크) 어조를 탑재한 Gemini LLM으로 06번 성향 분석(`aiAnalysis`) 및 07번 처방(`prescription`)을 생성함. 또한 미독서 장르(`unreadGenres`) 맞춤 추천과 국립중앙도서관 실존 서지 검증 + 교보 CDN 표지를 바인딩하여 1개의 통합 응답으로 서빙하도록 설계함. |
| **2026-09-15** | **사서 4종 페르소나 전면 고도화, 종결어미(~냥, ~두둥, ~누누, ~크크) 절제 규칙 및 기본 표시명 '누디' 동기화** | 토론자 4인과 동일한 수준의 프로덕션 표준 템플릿(MBTI, 담당 장르, 독서 성향, 말투 규칙, 종결어미 규칙, 사용자 정의 애칭 처리, 도구 지침)을 확립하여 캐릭터의 생동감과 전문성을 극대화하고, 기계적 어미 남발로 인한 톤 붕괴를 막기 위해 응답당 1~2회 핵심 문장에만 종결어미를 절제하여 사용하도록 설계함. 또한 갯민숭달팽이의 기본 표시명을 '바다달팽이'에서 '누디'로 변경하고 `backend-core-api`와 1:1 동기화하기 위함. |
| **2026-09-15** | **토론 기억 전용 테이블(`agent.debate_insights`) 분리 및 피날레 백그라운드 자동 벡터화 적재 채택** | 책 본문 발췌/인용구 수첩 전용인 `agent.scrap_vector`의 순수성과 데이터 정합성을 보존하고, 사용자와 AI 토론 파트너 간의 사유 결실(토론 요약 및 통찰)을 영구 자산화하기 위해 전용 테이블 및 HNSW 인덱스를 구축함. 또한 피날레 시 사용자 응답 지연(0ms)을 방지하기 위해 FastAPI `BackgroundTasks`로 비동기 벡터화/저장하고, `search_debate_memory` 도구를 `GENERIC_TOOLS`에 전사 바인딩하여 8개 페르소나 모두가 과거 토론 기억을 자연스럽게 회상할 수 있도록 설계함. |
| **2026-09-15** | **토론 피날레 4단계 플로우(`action: conclude`) 분리, 큐레이터 선위임 및 실존 도서 큐레이션 연계** | 도서 카드의 [등록] 버튼 클릭과 [토론 마무리]를 명확히 분리하여 등록 시 대화가 강제 종료되는 UX 어색함을 해소하고, UI 마무리 버튼 또는 자연어 마무리 발화 시 대화 히스토리의 논제 맥락을 추출하여 `curator_node`로 선위임함으로써 환각 없는 국립중앙도서관 실존 도서 및 고화질 표지를 피날레 총평과 함께 안전하게 제공하기 위함. |

| **2026-09-14** | **전사 중앙 인증(`core-api` JWT_SECRET 공유 서명 검증), 게스트 모드 바이패스 및 MSA Token Relay 채택** | `verify_signature=False`로 인한 심각한 보안 취약점(BOLA/토큰 위조)을 원천 차단하고, `core-api`의 `JWT_SECRET_KEY`를 공유받아 0ms 로컬 서명 검증을 수행하며, 비로그인 시 무작위 UUID 생성을 중단하고 게스트 모드(`member_id=None`)로 DB 조회를 스킵(Bypass)하여 비용과 일관성을 지키고, 서재 조회는 Token Relay로 `core-api`의 `GET /api/v1/library/books`를 호출하여 불필요한 내부 무인증 API 신설 없이 MSA 보안 경계를 일치시키기 위함. |
| **2026-09-14** | **4단계 다중 방어 보안 가드레일(0차 Auth ➔ 1차 Safety ➔ 2차 Input ➔ 3차 Security) 구축 및 8종 페르소나/SSE 구조로의 현대적 재구성** | 레거시(`backend-discovery`의 Bedrock 2종 사서)를 단순 복제하지 않고, 본 레포의 구조(8종 페르소나, LangGraph, SSE 스트리밍, $0 무과금)에 맞게 파이썬 고속 정규식 게이트(0ms 무지연) 및 `app/domain/guardrails/` 모듈로 재설계하여 LLM 비용 $0 원천 방어, 109 핫라인 공감 안내, 자모/숫자/이모지 처리, 탈옥/PII 사전 차단을 달성하기 위함. |
| **2026-09-14** | **국립도서관 표지 누락 시 교보문고 고화질 CDN(458px) 0ms 무지연 자동 폴백 및 서지 메타데이터 정제 채택** | 서버 측 추가 HTTP HEAD/GET 유효성 검사로 인한 레이턴시(50~150ms)를 원천 차단하고 순수 ISBN 문자열 조합(0ms)으로 프론트에 즉시 전달하며, 복잡한 도서관 저자 표기('저자 : 헤세;역자 : 서상원;')를 순수 저자명으로 정제하고 KDC/PAGE를 파싱하여 프론트엔드 원클릭 서재 등록 완성도를 극대화하기 위함. |
| **2026-09-14** | **Supabase 공용 DB MSA 스키마 격리(`agent`) 및 Transaction Pooler(포트 6543) 연동** | 전사 $0 무과금 단일 Supabase Postgres 인스턴스 공유 정책 준수, 타 스키마(`core`/`record`) 직접 쿼리 원천 차단(REST API 원칙), Transaction Pooler prepared statement 충돌 방지(`statement_cache_size: 0`), pgvector 코사인 유사도 검색 최적화를 위함. |
| **2026-09-13** | **도서 추천 의도 감지 시 사서 노드에서 큐레이터 서브에이전트로 선위임 및 메시지 정제** | 사서 노드에서 불필요한 1차 LLM 호출(비용/지연 2~3초)을 제거하고, `tool_calls` 생성 후 `ToolMessage` 부재로 인한 LLM API의 400 Bad Request 에러를 원천 차단하기 위함. |
| **2026-09-12** | **DPYB 중앙 Reusable Workflow 연동 및 Git/PR 머지 표준 확정** | 조직 전체 CI/CD 일관성 유지(Python 3.12 CI, PR Lint) 및 `feat/*` 단일화, `[scope]` 대괄호 규격, 사람에 의한 안전한 최종 머지 원칙을 보장하기 위함. |
| **2026-09-12** | **바이브 코딩 하네스 표준 구축** | 복수의 AI 코딩 툴(Antigravity, Claude Code, Codex, Kiro) 간 컨텍스트 동기화 및 단일 책임 문서 관리 원칙을 준수하기 위함. |
| **2026-09-12** | **도서 바코드 스캔 및 Clova OCR을 `backend-ai-agent`에서 무상태(Stateless)로 전담** | `pyzbar`, `Pillow`, `libzbar0` 등 무거운 C-익스텐션 의존성을 `backend-core-api`로부터 격리하여 비즈니스 코어 서버를 가볍고 안전하게 유지하기 위함. |
| **2026-09-12** | **Dockerfile 동적 `$PORT` 바인딩 및 핫리로드 활성화** | Render(임의 포트) 및 Google Cloud Run(포트 8080 주입)의 제로비용 무과금 배포 정책을 단일 Dockerfile로 만족시키기 위함. |
| **2026-09-11** | **사서 페르소나 4종을 `backend-core-api` DB ENUM(`core.librarian_type`)과 1:1 일치** | 사서 종류(고양이, 슈빌, 바다달팽이, 게코)를 DB 스키마와 완벽 일치시키고, 사용자가 지어준 애칭(`librarian_name`)으로 호칭할 수 있게 유연성을 부여하기 위함. |
| **2026-09-11** | **Handoff 시 `summarizer_node`를 통한 어조 오염 방지** | 이전 페르소나의 강한 어조(사서의 귀여움/강사의 박력 등)가 다음 페르소나에게 누적 전이되는 프롬프트 오염 현상을 차단하고 핵심 질문/팩트만 전달하기 위함. |
| **2026-09-11** | **Supabase pgvector `scrap_vector`를 개인화 독서 기억 전용으로 한정** | 도서 추천은 메타데이터 기반이 적합하며, 벡터 DB는 사용자가 직접 남긴 문장/메모에 대한 의미론적 회상에 집중하여 토큰 및 검색 비용을 최소화하기 위함. |
| **2026-09-11** | **LangGraph 기반 8-Node 멀티 페르소나 아키텍처 채택** | 단일 LLM 프롬프트 분기 대비 상태 전이, 도구 호출 제어, 페르소나 간 인수인계(Handoff)의 결정론적 제어가 용이하기 때문. |
