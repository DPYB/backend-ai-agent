# PLAN.md — 미완료 작업 계획

> 현재 진행 중이거나 사용자 컨펌 후 바로 착수할 작업 체크리스트만 유지합니다. (완료된 작업은 `STATE.md`로 이동 후 본 문서에서 삭제)

---

## 미완료 작업 체크리스트

### 📌 Milestone 4 (Phase 20 E2E 통합 검증): 프론트엔드 연동 후 3대 서비스 통합 스모크 테스트
> **백엔드(backend-ai-agent) 구현 및 126개 단위 테스트, Ruff, Mypy 자가 검증 100% 완료 상태**

- [ ] **E2E 스모크 테스트 1: 날씨 뱃지 (Signals 연동)**
  - 브라우저 위치 허용 시 `/api/v1/chat` 응답의 `signals` 객체(날씨/기온/시간대/무드)가 프론트엔드 상단 `WeatherMoodBadge`에 정상 렌더링되는지 확인

- [ ] **E2E 스모크 테스트 2: 도서 추천 카드 메타데이터**
  - 도서 추천 발화 시 표지 이미지(`cover_url`), 장르(국문/영문 유연 지원), 총 쪽수(`page_count`)가 `BookCardView`에 올바르게 표시되고 '내 서재에 담기' 원클릭 등록이 정상 동작하는지 확인

- [ ] **E2E 스모크 테스트 3: 빈 서재 정직 응답**
  - 서재가 비어있는 상태에서 "내 서재에 무슨 책 있어?" 질문 시 더 이상 '듄', '프로젝트 헤일메리'를 거짓으로 지어내지 않고 "등록된 도서가 없습니다"로 정직하게 답변하는지 확인

- [ ] **E2E 스모크 테스트 4: 토론 모드 평론가(이동진 톤) 페르소나 및 팩트 그라운딩**
  - 평론가 선택 후 토론 시 고양이 말투(~냥) 오염 없이 비평가 특유의 어조로 답변하는지, 대상 도서의 실제 서지/줄거리를 바탕으로 대화가 진행되는지 확인



---

### 📌 Milestone 6 (Phase 24): 국립도서관 다중 판본 표지·쪽수 생존 우선 매칭 및 등록 화면 장르 보존
> **《지구 끝의 온실》 표지 누락(34,150B 플레이스홀더) 극복, 쪽수 있는 판본 우선 선정, 등록 폼 장르 안정화**

- [ ] **1. 국립도서관 클라이언트 표지/쪽수 생존 판본 우선 선택 (`app/infrastructure/national_library_client.py`)**
  - `search_book` 반복문에서 첫 번째 판본이 표지가 죽어있거나(34,150B) 쪽수가 비어있을 때 바로 반환하지 않고, 상위 후보군(ranked_docs[:5]) 중 **실제 표지가 살아있고 쪽수(`PAGE`)가 존재하는 정식 판본**을 최우선 선택하도록 루프 및 우선순위 개선.
- [ ] **2. 도서 등록 화면(`RegisterBook.jsx`) 장르 보존 및 재분류 간섭 방지 (`frontend-reader-web`)**
  - AI 추천으로 넘어온 경우(`fromAIRecommendation`), 추천 시 검증된 표준 장르(`LITERATURE` 등)를 그대로 유지하고, 백그라운드 재분류(`autoClassifyGenre`)가 이를 덮어쓰거나 '기술과학'으로 오분류하지 않도록 방어.
- [ ] **3. 단위 테스트 작성 및 자가 검증 (Pytest, Ruff, Mypy)**
  - 《지구 끝의 온실》, 《어린 왕자》 다중 판본 표지 생존 선택 단위 테스트 추가 및 전체 테스트 100% 통과 검증.



